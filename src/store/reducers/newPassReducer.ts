const initState: newPassStateType = ''
export const newPassReducer = (state: newPassStateType = initState, action: ActionType): newPassStateType => {
    switch (action.type) {
        default: return state
    }
}
type newPassStateType = ''
type ActionType = { type: any }