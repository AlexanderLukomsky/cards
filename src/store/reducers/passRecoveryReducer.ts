const initState: passRecoveryStateType = ''
export const passRecoveryReducer = (state: passRecoveryStateType = initState, action: ActionType): passRecoveryStateType => {
    switch (action.type) {
        default: return state
    }
}
type passRecoveryStateType = ''
type ActionType = { type: any }