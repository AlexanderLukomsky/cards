const initState: registrationReducer = ''
export const registrationReducer = (state: registrationReducer = initState, action: ActionType): registrationReducer => {
    switch (action.type) {
        default: return state
    }
}
type registrationReducer = ''
type ActionType = { type: any }